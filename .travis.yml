sudo: false # use containers

language: python

addons:
  mariadb: '10.1'
  
before_install:
- mysql -u root -e "CREATE DATABASE travis;"
- mysql -u root -D travis < conf/schema.sql

before_script:
- cp conf/defaults.example.cfg conf/defaults.cfg
    